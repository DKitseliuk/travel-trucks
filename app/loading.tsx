import styles from './loading.module.css';

const Loading = () => {
  return (
    <section>
      <div className="container">
        <h1>Loading...</h1>
      </div>
    </section>
  );
};

export default Loading;
